query ImageQuery($id: ID, $size: String) {
    Image(id: $id) {
        title
        image(size: $size) {
            size(name: $size) {
                src
            }
            height
            width
        }
    }
}